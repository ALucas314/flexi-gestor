[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npx prisma generate']

[start]
cmd = 'npx prisma db push --accept-data-loss && npx tsx server/src/server.ts'

